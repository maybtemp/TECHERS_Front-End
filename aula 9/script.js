EventTarget.addEventListener("submit", (event) =>{
event.preventDefault()
const text = document.getElementbyId(div)
const button = document.getElemendtbyId(botao)
const title = getElementbyId(titulo)


     if(button.click) 
     {
          const notas = [text, title]
          document.createElement(document.getElementById(card))
     }
     


} )